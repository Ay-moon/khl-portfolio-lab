# Step 1 — Local Setup

## Goal
Run the project locally with reproducible environment.

## Tasks
- Create repo folders
- Create Python virtual environment
- Install dependencies
- Configure .env file
- Connect to local SQL Server

## Definition of Done
- Database reachable
- Tables can be created
- Seed script runs successfully
